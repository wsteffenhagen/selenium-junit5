# Reference - Do Not Edit
config {
  # Default browser config
  current = ${config.L1}

  # Config Presets
  L1 = ${config.local.chrome}
  L2 = ${config.local.edge}
  L3 = ${config.local.firefox}

  # Config Settings
  local {
    chrome {
      browser = "Chrome"
    }
    edge {
      browser = "Edge"
    }
    firefox {
      browser = "Firefox"
    }
  }

  # Global Variables
  baseurl = "https://www.google.com"
}